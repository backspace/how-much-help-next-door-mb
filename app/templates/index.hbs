<RequestChart
  @requests={{@model}}
  @activeCategoryIds={{this.categoriesArray}}
  @activeStatusIds={{this.statusesArray}}

  @toggleCategory={{this.toggleCategory}}
  @toggleStatus={{this.toggleStatus}}
/>